/** Bucket público de Storage para flyers de Evento (no reusar avatares). */
export const EVENTO_FLYER_BUCKET = "evento-flyers";

/** Tamaño máximo de archivo antes de comprimir (bytes). */
export const EVENTO_FLYER_MAX_BYTES = 5 * 1024 * 1024;

/** Lado máximo tras redimensionar (banner landscape). */
export const EVENTO_FLYER_MAX_SIDE = 1600;
